import React from 'react';
import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function QRScanner() {
  return (
    <PlaceholderPage
      title="QR Scanner"
      description="Scan QR codes on machines for quick access to machine details, inventory, and maintenance tasks."
      icon="📷"
    />
  );
}
