import React from 'react';
import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function Incidents() {
  return (
    <PlaceholderPage
      title="Incidents"
      description="Track and manage incidents, machine failures, and resolution processes."
      icon="⚠️"
    />
  );
}
