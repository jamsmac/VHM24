# VendHub Manager - Обзор системы

## Введение

VendHub Manager — это профессиональная ERP-система для управления вендинговой сетью кофейных автоматов. Система разработана с учётом реальных потребностей бизнеса и построена на принципе контроля через операции персонала.

## Целевая аудитория

- **Владельцы вендингового бизнеса** - полный контроль над сетью
- **Операторы** - эффективное выполнение задач через мобильное приложение
- **Техники** - управление ремонтами и обслуживанием
- **Инкассаторы** - безопасный сбор денежных средств
- **Администраторы** - аналитика и принятие решений

## Ключевые проблемы, которые решает система

### 1. Мониторинг оборудования
**Проблема**: Не понятно, когда аппарат вышел из строя
**Решение**: Connectivity Status с автоматическими алертами при отключении > 1 часа

### 2. Контроль качества обслуживания
**Проблема**: Операторы могут забывать о задачах или выполнять их некачественно
**Решение**:
- SLA контроль для всех типов задач
- Обязательные фото ДО и ПОСЛЕ
- Автоматический рейтинг операторов
- Система инцидентов

### 3. Учёт товаров
**Проблема**: Непонятно где находятся товары (на складе, у оператора, в аппарате)
**Решение**: 3-уровневая система остатков с автоматическим резервированием

### 4. Обслуживание компонентов
**Проблема**: Бункеры нужно мыть регулярно, но легко забыть
**Решение**: Автоматический контроль с графиком мойки и авто-задачами

### 5. Обратная связь от клиентов
**Проблема**: Клиенты не могут пожаловаться на проблемы
**Решение**: QR-коды на аппаратах для быстрых жалоб с SLA 2 часа

### 6. Финансовая прозрачность
**Проблема**: Нет понимания реальной рентабельности с учётом амортизации
**Решение**: Автоматический расчёт амортизации оборудования

## Архитектура системы

### Уровни системы

```
┌─────────────────────────────────────────────────┐
│          1️⃣ ФУНДАМЕНТ (Foundation)              │
│  - Глобальные настройки (с SLA)                 │
│  - Справочники (30+ системных)                  │
│  - Пользователи и роли (RBAC)                   │
└─────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│           2️⃣ ЯДРО (Core Modules)                │
│  - Аппараты (+ Connectivity Status)             │
│  - Номенклатура (+ контроль использования)      │
│  - Задачи (+ SLA, правила, чат)                 │
│  - Локации                                      │
└─────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│        3️⃣ ВАЖНЫЕ МОДУЛИ (Important)             │
│  - Продажи и импорт                             │
│  - Остатки 3-уровня (машины/склады/операторы)  │
│  - Контрагенты                                  │
│  - Оборудование (компоненты + запчасти)         │
└─────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│       4️⃣ ПРОДВИНУТЫЕ (Advanced)                 │
│  - Склад и партии (FIFO)                        │
│  - Финансы (+ амортизация)                      │
└─────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│         5️⃣ НОВЫЕ МОДУЛИ (New Modules)           │
│  - 🆕 Инциденты                                  │
│  - 🆕 Клиентские жалобы                          │
│  - 🆕 Рейтинг операторов                         │
│  - 🆕 Контроль обслуживания                      │
└─────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│    ПОДДЕРЖИВАЮЩИЕ (Supporting Modules)          │
│  - Файлы и медиа (с тегами)                     │
│  - Audit Logs (полное логирование)              │
│  - Уведомления (multi-channel)                  │
└─────────────────────────────────────────────────┘
```

## Основные потоки данных

### Поток 1: Пополнение аппарата

```
1. Система обнаруживает низкий остаток
   → Автоматическое создание задачи "Пополнение"

2. Задача назначается оператору
   → Уведомление в Telegram
   → Резервирование товаров на складе

3. Оператор берёт товар со склада
   → Остатки склада уменьшаются
   → Остатки "в пути" у оператора увеличиваются

4. Оператор пополняет аппарат
   → Обязательные фото ДО и ПОСЛЕ
   → Заполнение фактических количеств
   → Отметка в чек-листе

5. Задача закрывается
   → Остатки в аппарате обновляются
   → Остатки "в пути" уменьшаются
   → Движение товаров фиксируется
   → SLA отмечается как выполненный
```

### Поток 2: Инкассация

```
1. cash_in_machine > порог (например, 500,000 сум)
   → Автоматическое создание задачи "Инкассация"

2. Задача назначается инкассатору
   → Уведомление + ожидаемая сумма

3. Инкассатор выполняет сбор
   → ОБЯЗАТЕЛЬНЫЕ фото счётчика и денег
   → Внесение фактической суммы

4. Система проверяет расхождение
   → Если > 10% → создание инцидента
   → Если < 10% → норма

5. Задача закрывается
   → Создаётся финансовая операция (income)
   → cash_in_machine = 0
   → SLA отмечается
```

### Поток 3: Мойка компонента

```
1. Система каждый день проверяет
   → next_cleaning_due < today
   → Автоматическое создание задачи "Мойка компонента"

2. Задача назначается технику
   → Указан serial_number компонента (BUN-001)

3. Техник демонтирует компонент
   → Статус компонента: being_cleaned
   → Фото ДО мойки

4. Техник моет компонент
   → Фото ПОСЛЕ мойки

5. Задача закрывается
   → last_cleaning_date = today
   → cleaning_count++
   → next_cleaning_due = today + interval
   → Статус: in_storage или устанавливается в аппарат
```

### Поток 4: Клиентская жалоба

```
1. Клиент сканирует QR на аппарате
   → Открывается форма жалобы
   → Выбор типа проблемы
   → Опционально: фото, контакт

2. Система получает жалобу
   → status = new
   → response_deadline = NOW() + 2 часа
   → Уведомление оператору аппарата

3. Оператор реагирует (< 2 часов)
   → status = acknowledged
   → Может создать задачу (пополнение/ремонт)
   → Отправка ответа клиенту

4. Проблема решается
   → Выполнение задачи
   → status = resolved
   → Уведомление клиенту

5. Клиент оценивает (1-5 звёзд)
   → Жалоба закрывается
   → Данные идут в рейтинг оператора
```

## Автоматизация

### Автоматические задачи

| Триггер | Задача | SLA |
|---------|--------|-----|
| current_quantity < min_quantity | Пополнение | 12 часов |
| cash_in_machine > порог | Инкассация | 24 часа |
| next_cleaning_due < today | Мойка компонента | 3-14 дней |
| last_maintenance + interval < today | ТО | - |
| error_codes не пустой | Ремонт | 48 часов |

### Автоматические инциденты

| Триггер | Тип инцидента | Приоритет |
|---------|---------------|-----------|
| Расхождение при инкассации > 10% | money_discrepancy | high |
| Аппарат offline > 24 часа | machine_breakdown | medium |
| Задача просрочена > 48 часов | operator_violation | medium |
| Повторяющиеся ошибки (>5 за день) | component_failure | high |
| Просроченный товар не списан | quality_issue | high |

### Автоматические уведомления

- **Telegram** - основной канал (срочные)
- **Email** - важные уведомления
- **Push** - мобильные уведомления
- **SMS** - критичные алерты (редко, дорого)

## SLA метрики

### Операционные SLA

| Процесс | SLA | Контроль |
|---------|-----|----------|
| Пополнение | 12 часов | Автоматический |
| Инкассация | 24 часа | Автоматический |
| Мойка бункера (молоко) | 3 дня | Автоматический |
| Мойка бункера (кофе) | 7 дней | Автоматический |
| Мойка гриндера | 10 дней | Автоматический |
| Мойка варочной группы | 14 дней | Автоматический |
| Ремонт | 48 часов | Автоматический |
| ТО | По графику | Автоматический |

### Клиентские SLA

| Процесс | SLA | Контроль |
|---------|-----|----------|
| Реакция на жалобу | 2 часа | Автоматический |
| Решение жалобы | 24 часа | Автоматический |
| Ответ на инцидент (critical) | 30 минут | Автоматический |
| Ответ на инцидент (high) | 2 часа | Автоматический |

## Роли и доступ (RBAC)

### SuperAdmin
- **Доступ**: Полный контроль над системой
- **Возможности**: Все операции без ограничений
- **2FA**: Обязательно

### Admin
- **Доступ**: Управление системой, финансы, отчёты
- **Возможности**:
  - Управление всеми модулями
  - Финансовые отчёты
  - Рейтинги операторов
  - Инциденты
- **2FA**: Обязательно

### Operator
- **Доступ**: Свои задачи, свои аппараты
- **Возможности**:
  - Просмотр назначенных задач
  - Выполнение пополнения
  - Отложить задачу (с причиной)
  - Чат по задаче
  - Просмотр своего рейтинга

### Collector
- **Доступ**: Инкассация
- **Возможности**:
  - Задачи инкассации
  - Внесение сумм
  - Фото отчёты

### Technician
- **Доступ**: Ремонт, ТО, мойка
- **Возможности**:
  - Задачи ремонта и ТО
  - Управление компонентами
  - Использование запчастей
  - Мойка компонентов

### Viewer
- **Доступ**: Только чтение
- **Возможности**: Просмотр отчётов и дашбордов

## Безопасность

### Аутентификация
- JWT с access (15 мин) и refresh tokens (7 дней)
- 2FA для Admin и SuperAdmin (TOTP)
- Rate limiting: 100 запросов/минуту

### Аудит
- Полное логирование всех действий
- Партиционирование по месяцам
- Хранение: 3 месяца online, 12 месяцев архив

### Шифрование
- Пароли: bcrypt (cost 12)
- Чувствительные данные: AES-256
- HTTPS обязательно в production

## Масштабируемость

### Текущие возможности
- 1000+ аппаратов
- 100+ операторов
- 10,000+ товарных позиций
- 1M+ транзакций в месяц

### Оптимизация
- **БД**:
  - Индексы на всех foreign keys
  - Партиционирование транзакций и логов
  - Материализованные представления для отчётов
- **Кэш**:
  - Redis для справочников
  - Redis для сессий
- **Очереди**:
  - Bull/Celery для фоновых задач
  - Email/SMS рассылки
  - Импорт файлов

## Мониторинг

### Метрики
- Response time: p95 < 200ms
- Error rate: < 0.1%
- Uptime: 99.9%

### Алерты
- Аппарат offline > 1 час
- Ошибка rate > 1%
- Response time > 1s
- Disk usage > 80%
- Memory usage > 85%

## Интеграции

### Существующие
- **Telegram Bot** - основной интерфейс для операторов
- **Email** - уведомления
- **SMS** - критичные алерты

### Планируемые
- **1C** - бухгалтерский учёт
- **CRM** - управление клиентами
- **BI системы** - расширенная аналитика
- **Платёжные системы** - онлайн оплата

## Следующие шаги

См. [Roadmap](./roadmap.md) для детального плана разработки.
