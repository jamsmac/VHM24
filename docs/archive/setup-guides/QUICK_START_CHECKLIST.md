# ✅ Быстрый чеклист для запуска VendHub онлайн

## 🎯 За 30 минут до запуска онлайн

### ☑️ Что подготовить ПЕРЕД началом:

- [ ] **Сервер готов** (Ubuntu 22.04, 4+ CPU, 8GB RAM)
- [ ] **SSH доступ** к серверу (логин/пароль или SSH ключ)
- [ ] **Доменное имя** куплено (например, vendhub.uz)
- [ ] **DNS настроен** (A-запись указывает на IP сервера)
- [ ] **Email** для SSL сертификата
- [ ] **Telegram Bot Token** получен от @BotFather

---

## 🚀 Быстрый старт (3 команды)

### Вариант 1: Автоматическая установка (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Подключиться к серверу
ssh root@ваш-сервер-ip

# 2. Скачать проект
cd /opt && git clone https://github.com/jamsmac/VendHub.git && cd VendHub

# 3. Запустить установку
chmod +x deploy-production.sh && sudo ./deploy-production.sh
```

**Вас спросят:**
1. Домен → введите `vendhub.uz`
2. Email → введите `admin@example.com`
3. Telegram Token → вставьте токен от @BotFather
4. Пароль БД → придумайте сложный пароль
5. JWT Secret → придумайте или оставьте пустым (сгенерируется)

⏱️ **Время установки: 10-15 минут**

---

## 📋 Что происходит автоматически:

✅ Установка Docker и Docker Compose
✅ Настройка Firewall (UFW)
✅ Установка Fail2Ban (защита от брутфорса)
✅ Клонирование проекта
✅ Создание конфигурационных файлов (.env)
✅ Получение SSL сертификата (Let's Encrypt)
✅ Настройка Nginx (reverse proxy)
✅ Сборка и запуск всех сервисов
✅ Применение миграций базы данных
✅ Генерация VAPID ключей (Web Push)
✅ Настройка автоматических бэкапов (каждый день в 3:00)
✅ Настройка мониторинга (каждые 5 минут)

---

## ✅ Проверка после установки

### 1. Проверить доступность сайта

Откройте в браузере:
```
https://ваш-домен.com        ← Должна открыться главная страница
https://ваш-домен.com/api/docs  ← Swagger документация
https://ваш-домен.com/health    ← Должно вернуть {"status":"ok"}
```

### 2. Создать первого администратора

```bash
cd /opt/VendHub
docker-compose -f docker-compose.production.yml exec backend npm run create-admin
```

**Введите данные:**
- Email: admin@vendhub.com
- Пароль: (придумайте сложный)
- Имя: Admin
- Фамилия: Administrator

### 3. Войти в систему

1. Откройте https://ваш-домен.com
2. Войдите с email и паролем администратора
3. Проверьте, что все работает

### 4. Настроить Telegram бота

1. В админ-панели перейдите: `/telegram/settings`
2. Вставьте Bot Token
3. Нажмите "Сохранить"
4. Откройте бота в Telegram и отправьте `/start`

---

## 🔧 Полезные команды

### Просмотр логов

```bash
cd /opt/VendHub

# Все логи в реальном времени
docker-compose -f docker-compose.production.yml logs -f

# Только backend
docker-compose -f docker-compose.production.yml logs -f backend

# Последние 100 строк
docker-compose -f docker-compose.production.yml logs --tail=100
```

### Перезапуск сервисов

```bash
# Перезапуск всего
docker-compose -f docker-compose.production.yml restart

# Перезапуск backend
docker-compose -f docker-compose.production.yml restart backend
```

### Обновление проекта

```bash
cd /opt/VendHub
git pull
docker-compose -f docker-compose.production.yml up -d --build
```

### Бэкапы

```bash
# Создать бэкап вручную
/opt/VendHub/backup.sh

# Список бэкапов
ls -lh /opt/VendHub/backups/

# Бэкапы создаются автоматически каждый день в 3:00
```

### Проверка состояния

```bash
# Статус всех контейнеров
docker-compose -f docker-compose.production.yml ps

# Использование ресурсов
docker stats

# Место на диске
df -h
```

---

## 🆘 Что делать если что-то не работает

### Backend не запускается
```bash
docker-compose -f docker-compose.production.yml logs backend
docker-compose -f docker-compose.production.yml restart backend
```

### Сайт не открывается (502 Bad Gateway)
```bash
# Проверить что все контейнеры запущены
docker-compose -f docker-compose.production.yml ps

# Перезапустить Nginx
docker-compose -f docker-compose.production.yml restart nginx
```

### SSL сертификат не работает
```bash
# Проверить сертификат
sudo certbot certificates

# Обновить сертификат
sudo certbot renew
```

### База данных не работает
```bash
docker-compose -f docker-compose.production.yml restart postgres
docker-compose -f docker-compose.production.yml logs postgres
```

---

## 📞 Куда обратиться за помощью

1. **Подробная документация**: `PRODUCTION_SETUP_RU.md`
2. **Deployment Guide**: `.claude/deployment-guide.md`
3. **Логи системы**: `/var/log/vendhub-health.log`
4. **GitHub Issues**: Создайте issue в репозитории

---

## 💡 Советы

✅ **Сохраните пароли** в безопасном месте (1Password, LastPass)
✅ **Настройте мониторинг** через Telegram (получайте уведомления о проблемах)
✅ **Регулярно обновляйте** систему: `apt update && apt upgrade`
✅ **Проверяйте бэкапы** раз в неделю
✅ **Следите за местом** на диске: `df -h`

---

## 🎉 Готово!

После выполнения этих шагов ваш **VendHub Manager работает 24/7 онлайн**!

**Следующие шаги:**
1. ✅ Добавьте локации
2. ✅ Добавьте машины
3. ✅ Создайте пользователей (операторы, техники)
4. ✅ Добавьте номенклатуру (продукты)
5. ✅ Начните создавать задачи

**Удачи! 🚀**

---

**VendHub Manager** - Полное управление вендинговыми автоматами 24/7
